import 'package:flutter/material.dart';

// It's easier to just replicate these than
// try and introspect from pubspec.yaml

const APPNAME = "IDEA2.ART";
const APPVERSION = "0.0.1";

// Theme icons

class Idea2artIcons {
  static const auto = Icons.auto_fix_normal;
  static const create = Icons.add_circle_outline;
  static const variants = Icons.autorenew;
  static const fill = Icons.select_all;
  static const mask = Icons.brush;
  static const generate_more = Icons.auto_awesome_motion;
  static const delete = Icons.delete_forever;
  static const cancel = Icons.cancel;
  static const command_menu = Icons.menu;
}
